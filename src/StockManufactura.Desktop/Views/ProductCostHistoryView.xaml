<UserControl x:Class="StockManufactura.Desktop.Views.ProductCostHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1200">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Historial de Costos por Producto" FontSize="24" FontWeight="Bold" />

        <Grid Grid.Row="1" Margin="0 12 0 12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ComboBox Grid.Column="0" Margin="0 0 8 0" ItemsSource="{Binding Products}" SelectedItem="{Binding SelectedProduct}" DisplayMemberPath="Nombre" Height="34" />
            <ComboBox Grid.Column="1" Margin="0 0 8 0" ItemsSource="{Binding History}" SelectedItem="{Binding SelectedOlder}" DisplayMemberPath="Fecha" Height="34" />
            <ComboBox Grid.Column="2" Margin="0 0 8 0" ItemsSource="{Binding History}" SelectedItem="{Binding SelectedNewer}" DisplayMemberPath="Fecha" Height="34" />
            <Button Grid.Column="3" Margin="0 0 8 0" Content="Cargar" Command="{Binding LoadHistoryCommand}" Height="34" />
            <Button Grid.Column="4" Content="Comparar" Command="{Binding CompareCommand}" Height="34" />
        </Grid>

        <DataGrid Grid.Row="2" ItemsSource="{Binding History}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha}" Width="150" />
                <DataGridTextColumn Header="Costo" Binding="{Binding CostoNuevo}" Width="120" />
                <DataGridTextColumn Header="Margen" Binding="{Binding MargenUtilizado}" Width="100" />
                <DataGridTextColumn Header="Precio Sugerido" Binding="{Binding PrecioSugeridoNuevo}" Width="140" />
                <DataGridTextColumn Header="Usuario" Binding="{Binding Usuario}" Width="120" />
                <DataGridTextColumn Header="Motivo" Binding="{Binding MotivoRecalculo}" Width="220" />
                <DataGridTextColumn Header="Cotización" Binding="{Binding CotizacionUtilizada}" Width="100" />
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="3" Text="{Binding ComparisonSummary}" FontWeight="SemiBold" Margin="0 10 0 0" TextWrapping="Wrap" />
    </Grid>
</UserControl>
