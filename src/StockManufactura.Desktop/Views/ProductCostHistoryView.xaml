<UserControl x:Class="StockManufactura.Desktop.Views.ProductCostHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Margin="20" Background="{StaticResource BackgroundSoftLightBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Encabezado -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0 0 0 12">
            <TextBlock Text="HISTORIAL DE PRODUCTO" Style="{StaticResource SectionTitleStyle}" VerticalAlignment="Center" />
            <Button Content="Volver" Command="{Binding BackCommand}" Width="100" Height="32" Margin="16 0 0 0" Style="{StaticResource SecondaryButtonStyle}" />
        </StackPanel>

        <!-- Selector de producto -->
        <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}" Margin="0 0 0 12" Padding="12 8">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Producto:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0 0 10 0" />
                <ComboBox ItemsSource="{Binding Products}"
                          SelectedItem="{Binding SelectedProduct}"
                          DisplayMemberPath="Nombre"
                          Width="380" Height="32" />
                <TextBlock Text="{Binding SelectedProduct.Codigo, StringFormat=  [{0}]}"
                           VerticalAlignment="Center" Margin="8 0 0 0"
                           Foreground="{StaticResource SecondaryAccentBrush}" />
            </StackPanel>
        </Border>

        <!-- Timeline -->
        <Border Grid.Row="2" Style="{StaticResource CardBorderStyle}">
            <DockPanel>
                <Border DockPanel.Dock="Top" Background="{StaticResource BackgroundDarkMetalBrush}" Padding="8" CornerRadius="4" Margin="0 0 0 8">
                    <TextBlock Text="Línea de tiempo — todos los cambios del producto" Foreground="{StaticResource SecondaryAccentBrush}" FontWeight="SemiBold" />
                </Border>
                <DataGrid ItemsSource="{Binding Timeline}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          AlternatingRowBackground="#F5F5F5">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha}" Width="145" />
                        <DataGridTextColumn Header="Tipo de evento" Binding="{Binding TipoEvento}" Width="155" />
                        <DataGridTextColumn Header="Descripción" Binding="{Binding Descripcion}" Width="*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="TextWrapping" Value="Wrap" />
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Usuario" Binding="{Binding Usuario}" Width="130" />
                        <DataGridTextColumn Header="Costo nuevo" Binding="{Binding CostoNuevo}" Width="110" />
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>

        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Margin="0 10 0 0" Style="{StaticResource StatusMessageStyle}" />
    </Grid>
</UserControl>
