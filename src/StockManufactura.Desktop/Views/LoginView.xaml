<UserControl x:Class="StockManufactura.Desktop.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="360" d:DesignWidth="520">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="StockManufactura" FontSize="28" FontWeight="Bold" Margin="0 0 0 24" />
        <TextBlock Text="Acceso al sistema" FontSize="16" Margin="0 0 0 32" Grid.Row="1" />

        <StackPanel Grid.Row="2" Orientation="Vertical" Margin="0 8 0 0">
            <TextBlock Text="Email" Margin="0 0 0 4" />
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Height="35" />

            <TextBlock Text="Contraseña" Margin="0 16 0 0 4" />
            <PasswordBox x:Name="PasswordBox" Height="35" PasswordChanged="OnPasswordChanged" />

            <Button Content="Ingresar" Command="{Binding LoginCommand}" Height="40" Margin="0 24 0 0" />
            <TextBlock Text="{Binding StatusMessage}" Foreground="Red" Margin="0 12 0 0" />
        </StackPanel>
    </Grid>
</UserControl>
