<UserControl x:Class="StockManufactura.Desktop.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="360" d:DesignWidth="520">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </UserControl.Resources>
    <Grid Margin="24" Background="{StaticResource BackgroundSoftLightBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="INTEGRA MANUFACTURING" Style="{StaticResource ScreenTitleStyle}" Margin="0 0 0 24" />
        <TextBlock Text="Acceso al sistema" FontSize="16" FontFamily="Georgia" FontStyle="Italic" Foreground="{StaticResource TextMutedBrush}" Margin="0 0 0 32" Grid.Row="1" />

        <Border Grid.Row="2" Style="{StaticResource CardBorderStyle}">
            <StackPanel Orientation="Vertical" Margin="0 4 0 0">
            <TextBlock Text="Email" Margin="0 0 0 4" />
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Height="35" />

            <TextBlock Text="Contraseña" Margin="0 16 0 4" />
            <PasswordBox x:Name="PasswordBox" Height="35" PasswordChanged="OnPasswordChanged" />

            <StackPanel Visibility="{Binding RequiresPasswordChange, Converter={StaticResource BoolToVisibilityConverter}}" Margin="0 16 0 0">
                <TextBlock Text="Nueva contraseña" Margin="0 0 0 4" />
                <PasswordBox x:Name="NewPasswordBox" Height="35" PasswordChanged="OnNewPasswordChanged" />

                <TextBlock Text="Confirmar nueva contraseña" Margin="0 12 0 4" />
                <PasswordBox x:Name="ConfirmPasswordBox" Height="35" PasswordChanged="OnConfirmNewPasswordChanged" />

                <Button Content="Cambiar contraseña"
                        Command="{Binding ChangePasswordCommand}"
                        Margin="0 12 0 0"
                        Style="{StaticResource PrimaryButtonStyle}" />
            </StackPanel>

            <Button Content="Ingresar" Command="{Binding LoginCommand}" Height="40" Margin="0 24 0 0" Style="{StaticResource PrimaryButtonStyle}" />
            <TextBlock Text="{Binding StatusMessage}" Margin="0 12 0 0" Style="{StaticResource StatusMessageStyle}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
