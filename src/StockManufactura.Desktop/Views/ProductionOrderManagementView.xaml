<UserControl x:Class="StockManufactura.Desktop.Views.ProductionOrderManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="980">
    <Grid Background="{StaticResource BackgroundSoftLightBrush}">
        <Grid Margin="22 18 18 18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0 0 0 14">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="INTEGRA MANUFACTURING" Style="{StaticResource ScreenTitleStyle}" />
                    <TextBlock Text="Sistema de Gestion Integral de Inventario, Produccion y Costos"
                               FontFamily="Georgia"
                               FontStyle="Italic"
                               Foreground="{StaticResource TextMutedBrush}"
                               FontSize="16" />
                </StackPanel>

                <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                    <TextBlock Text="{Binding CurrentUserName, StringFormat=Usuario: {0}}" FontWeight="SemiBold" HorizontalAlignment="Right" />
                    <Border Background="#E7F4E8" BorderBrush="#9FC59C" BorderThickness="1" CornerRadius="8" Padding="10 4" Margin="0 6 0 0">
                        <TextBlock Text="{Binding FactoryConnectionStatus, StringFormat=Servidor Fabrica: {0}}" Foreground="#2E7D32" FontWeight="SemiBold" />
                    </Border>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1" Margin="0 0 0 14">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}" Margin="0 0 10 0">
                    <StackPanel>
                        <TextBlock Text="STOCK CRITICO" Foreground="{StaticResource PrimaryAccentBrush}" FontWeight="Bold" FontSize="18" />
                        <Border Background="#F3F1EC" CornerRadius="8" Padding="10" Margin="0 10 0 0">
                            <TextBlock Text="{Binding CriticalStockCount, StringFormat=STOCK MINIMO ({0} items)}" Foreground="{StaticResource SecondaryAccentBrush}" FontWeight="SemiBold" FontSize="17" />
                        </Border>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource CardBorderStyle}" Margin="5 0 5 0">
                    <StackPanel>
                        <TextBlock Text="ORDENES DE PRODUCCION" Foreground="{StaticResource SecondaryAccentBrush}" FontWeight="Bold" FontSize="18" />
                        <Border Background="#F3F1EC" CornerRadius="8" Padding="10" Margin="0 10 0 0">
                            <TextBlock Text="{Binding OrdersInProcessCount, StringFormat=EN PROCESO ({0})}" Foreground="{StaticResource PrimaryAccentBrush}" FontWeight="SemiBold" FontSize="17" />
                        </Border>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}" Margin="10 0 0 0">
                    <StackPanel>
                        <TextBlock Text="COSTOS TOTALES MES" Foreground="{StaticResource PrimaryAccentBrush}" FontWeight="Bold" FontSize="18" />
                        <TextBlock Text="{Binding EstimatedOrdersCost, StringFormat=$ {0:N2}}" FontWeight="Bold" FontSize="37" Margin="0 14 0 0" HorizontalAlignment="Center" />
                    </StackPanel>
                </Border>
            </Grid>

            <Border Grid.Row="2" Style="{StaticResource CardBorderStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Modulo: Ordenes de Produccion" FontSize="36" FontWeight="Bold" Margin="0 0 0 10" />

                    <Grid Grid.Row="1" Margin="0 0 0 8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.4*" />
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="1.2*" />
                            <ColumnDefinition Width="1.2*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0 0 8 0">
                            <TextBlock Text="Código orden" />
                            <TextBox Text="{Binding CodigoInput, UpdateSourceTrigger=PropertyChanged}"
                                     Height="34" ToolTip="Dejá vacío para generar automáticamente. Ingresá el mismo código para agrupar varios productos en una orden." />
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="0 0 8 0">
                            <TextBlock Text="Producto" />
                            <ComboBox ItemsSource="{Binding Products}" SelectedItem="{Binding SelectedProduct}" DisplayMemberPath="Nombre" Height="34" />
                        </StackPanel>
                        <StackPanel Grid.Column="2" Margin="0 0 8 0">
                            <TextBlock Text="Observaciones" />
                            <TextBox Text="{Binding Observaciones, UpdateSourceTrigger=PropertyChanged}" Height="34" />
                        </StackPanel>
                        <StackPanel Grid.Column="3" Margin="0 0 8 0">
                            <TextBlock Text="Cant. planeada" />
                            <TextBox Text="{Binding CantidadPlaneada, UpdateSourceTrigger=PropertyChanged}" Height="34" />
                        </StackPanel>
                        <StackPanel Grid.Column="4">
                            <TextBlock Text="Cant. producida" />
                            <TextBox Text="{Binding CantidadProducida, UpdateSourceTrigger=PropertyChanged}" Height="34" />
                        </StackPanel>
                    </Grid>

                    <DataGrid Grid.Row="2"
                              ItemsSource="{Binding Orders}"
                              SelectedItem="{Binding SelectedOrder}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              HeadersVisibility="Column"
                              GridLinesVisibility="Horizontal"
                              BorderBrush="{StaticResource SecondaryAccentBrush}"
                              BorderThickness="1"
                              RowHeight="40"
                              MinHeight="260"
                              Margin="0 0 0 10">
                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="{StaticResource BackgroundDarkMetalBrush}" />
                                <Setter Property="Foreground" Value="#FFFFFF" />
                                <Setter Property="FontWeight" Value="Bold" />
                                <Setter Property="BorderThickness" Value="0" />
                                <Setter Property="Padding" Value="8,4" />
                            </Style>
                            <Style TargetType="DataGridCell">
                                <Setter Property="BorderThickness" Value="0" />
                                <Setter Property="Padding" Value="8,4" />
                            </Style>
                        </DataGrid.Resources>
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="White" />
                                <Setter Property="Foreground" Value="{StaticResource TextMainBrush}" />
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#F6EEF3" />
                                        <Setter Property="Foreground" Value="{StaticResource TextMainBrush}" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID ORDEN" Binding="{Binding Codigo}" Width="1.3*" />
                            <DataGridTextColumn Header="PRODUCTO ID" Binding="{Binding ProductoId}" Width="1.8*" />
                            <DataGridTextColumn Header="CANTIDAD" Binding="{Binding CantidadPlaneada, StringFormat=N2}" Width="1*" />
                            <DataGridTextColumn Header="ESTADO" Binding="{Binding Estado}" Width="1.2*" />
                            <DataGridTextColumn Header="FECHA ENTREGA" Binding="{Binding FechaFin, StringFormat=yyyy-MM-dd}" Width="1.2*" />
                            <DataGridTextColumn Header="COSTO" Binding="{Binding CantidadProducida, StringFormat=N2}" Width="1*" />
                        </DataGrid.Columns>
                    </DataGrid>

                    <WrapPanel Grid.Row="3" Margin="0 0 0 8">
                        <Button Content="Crear" Command="{Binding CreateOrderCommand}" Width="95" Margin="0 0 8 8" Style="{StaticResource PrimaryButtonStyle}" />
                        <Button Content="Planificar" Command="{Binding PlanifyCommand}" Width="98" Margin="0 0 8 8" Style="{StaticResource SecondaryButtonStyle}" />
                        <Button Content="Iniciar" Command="{Binding StartCommand}" Width="90" Margin="0 0 8 8" Style="{StaticResource PrimaryButtonStyle}" />
                        <Button Content="Registrar" Command="{Binding RegisterProductionCommand}" Width="98" Margin="0 0 8 8" Style="{StaticResource PrimaryButtonStyle}" />
                        <Button Content="Borrador" Command="{Binding SetDraftCommand}" Width="95" Margin="0 0 8 8" Style="{StaticResource SecondaryButtonStyle}" />
                        <Button Content="Cancelar" Command="{Binding CancelCommand}" Width="95" Margin="0 0 8 8" Style="{StaticResource SecondaryButtonStyle}" />
                    </WrapPanel>

                    <Grid Grid.Row="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Content="Confirmar Orden" Width="180" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding CompleteCommand}" />
                        <TextBlock Grid.Column="1" Text="{Binding StatusMessage}" Margin="12 8" Style="{StaticResource StatusMessageStyle}" VerticalAlignment="Center" />
                        <Button Grid.Column="2" Content="Generar Reporte" Width="180" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding LoadCommand}" />
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
