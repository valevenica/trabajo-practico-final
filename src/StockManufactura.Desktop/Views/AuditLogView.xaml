<UserControl x:Class="StockManufactura.Desktop.Views.AuditLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1200">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Bitácora" FontSize="18" FontWeight="Bold" />

        <Grid Grid.Row="1" Margin="0 12 0 12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Margin="0 0 8 0" Text="{Binding Usuario, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Grid.Column="1" Margin="0 0 8 0" Text="{Binding Modulo, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Grid.Column="2" Margin="0 0 8 0" Text="{Binding Accion, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Grid.Column="3" Margin="0 0 8 0" Text="{Binding FromDate, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Grid.Column="4" Margin="0 0 8 0" Text="{Binding ToDate, UpdateSourceTrigger=PropertyChanged}" />
            <Button Grid.Column="5" Content="Buscar" Command="{Binding SearchCommand}" Height="34" />
        </Grid>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Items}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="FechaHora" Binding="{Binding FechaHora}" Width="150" />
                <DataGridTextColumn Header="Usuario" Binding="{Binding Usuario}" Width="120" />
                <DataGridTextColumn Header="Módulo" Binding="{Binding Modulo}" Width="130" />
                <DataGridTextColumn Header="Acción" Binding="{Binding Accion}" Width="130" />
                <DataGridTextColumn Header="Entidad" Binding="{Binding Entidad}" Width="120" />
                <DataGridTextColumn Header="IdEntidad" Binding="{Binding IdEntidad}" Width="160" />
                <DataGridTextColumn Header="Descripción" Binding="{Binding Descripcion}" Width="*" />
                <DataGridTextColumn Header="Equipo" Binding="{Binding Equipo}" Width="120" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
