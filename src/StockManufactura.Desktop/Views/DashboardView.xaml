<UserControl x:Class="StockManufactura.Desktop.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:StockManufactura.Desktop.Converters"
             mc:Ignorable="d"
             d:DesignHeight="360" d:DesignWidth="520">
    <UserControl.Resources>
        <converters:BoolToBrushConverter x:Key="BoolToBrushConverter" />
    </UserControl.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Panel de control" FontSize="24" FontWeight="Bold" />
        <TextBlock Grid.Row="1" Text="Bienvenido al sistema de manufactura" Margin="0 12 0 0" />

        <Border Grid.Row="2" Background="#F5F7FA" BorderBrush="#D0D7DE" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0 18 0 0">
            <StackPanel>
                <TextBlock Text="Estado del sistema" FontSize="16" FontWeight="SemiBold" />
                <TextBlock Text="{Binding StatusMessage}" Foreground="#C2410C" Margin="0 6 0 0" />
                <Grid Margin="0 10 0 0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*" />
                        <ColumnDefinition Width="1*" />
                        <ColumnDefinition Width="1*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Último backup" FontWeight="SemiBold" />
                    <TextBlock Grid.Column="1" Text="Última sincronización" FontWeight="SemiBold" />
                    <TextBlock Grid.Column="2" Text="Última actualización del dólar" FontWeight="SemiBold" />
                    <TextBlock Grid.Row="1" Text="{Binding LastBackupText}" Margin="0 4 0 0" />
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding LastDriveSyncText}" Margin="0 4 0 0" />
                    <TextBlock Grid.Row="1" Grid.Column="2" Text="{Binding LastDollarUpdateText}" Margin="0 4 0 0" />

                    <TextBlock Grid.Row="2" Text="Internet" FontWeight="SemiBold" Margin="0 12 0 0" />
                    <TextBlock Grid.Row="2" Grid.Column="1" Text="Tamaño DB" FontWeight="SemiBold" Margin="0 12 0 0" />
                    <TextBlock Grid.Row="2" Grid.Column="2" Text="Versión" FontWeight="SemiBold" Margin="0 12 0 0" />
                    <TextBlock Grid.Row="3" Text="{Binding InternetText}" Margin="0 4 0 0" />
                    <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding DatabaseSizeText}" Margin="0 4 0 0" />
                    <TextBlock Grid.Row="3" Grid.Column="2" Text="{Binding VersionText}" Margin="0 4 0 0" />
                </Grid>

                <StackPanel Orientation="Horizontal" Margin="0 14 0 0">
                    <Border Background="#E8F5E9" CornerRadius="6" Padding="10 6" Margin="0 0 8 0">
                        <TextBlock Text="{Binding ProductCountText, StringFormat='Productos: {0}'}" />
                    </Border>
                    <Border Background="#E3F2FD" CornerRadius="6" Padding="10 6" Margin="0 0 8 0">
                        <TextBlock Text="{Binding ResourceCountText, StringFormat='Recursos: {0}'}" />
                    </Border>
                    <Border Background="#FFF3E0" CornerRadius="6" Padding="10 6" Margin="0 0 8 0">
                        <TextBlock Text="{Binding CustomerCountText, StringFormat='Clientes: {0}'}" />
                    </Border>
                    <Button Content="Refrescar" Margin="8 0 0 0" Command="{Binding RefreshStatusCommand}" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0 12 0 0">
                    <Border CornerRadius="6" Padding="10 8" Margin="0 0 8 0" Background="{Binding BackupHealthy, Converter={StaticResource BoolToBrushConverter}, FallbackValue=#F3F4F6}">
                        <TextBlock Text="{Binding BackupStatusText, StringFormat='Backup: {0}'}" />
                    </Border>
                    <Border CornerRadius="6" Padding="10 8" Margin="0 0 8 0" Background="{Binding SyncHealthy, Converter={StaticResource BoolToBrushConverter}, FallbackValue=#F3F4F6}">
                        <TextBlock Text="{Binding CloudStatusText}" />
                    </Border>
                    <Border CornerRadius="6" Padding="10 8" Background="#F3F4F6">
                        <TextBlock Text="{Binding StatusToneText, StringFormat='Estado: {0}'}" />
                    </Border>
                </StackPanel>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0 18 0 0">
            <Button Content="Recursos"
                Width="160"
                Height="36"
                Margin="0 0 12 0"
                HorizontalAlignment="Left"
                Command="{Binding NavigateToResourcesCommand}" />
            <Button Content="Configuración Monetaria"
                Width="220"
                Height="36"
                HorizontalAlignment="Left"
                Command="{Binding NavigateToMonetaryConfigurationCommand}" />
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0 12 0 0">
            <Button Content="Bitácora"
                Width="160"
                Height="36"
                Margin="0 0 12 0"
                HorizontalAlignment="Left"
                Command="{Binding NavigateToAuditLogCommand}" />
            <Button Content="Backups"
                Width="160"
                Height="36"
                Margin="0 0 12 0"
                HorizontalAlignment="Left"
                Command="{Binding NavigateToBackupsCommand}" />
            <Button Content="Historial Costos"
                Width="180"
                Height="36"
                HorizontalAlignment="Left"
                Command="{Binding NavigateToProductCostHistoryCommand}" />
        </StackPanel>
    </Grid>
</UserControl>
